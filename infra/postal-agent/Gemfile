source 'https://rubygems.org'

# Sinatra and a server are the only additions. Everything else -- ActiveRecord,
# Postal's models -- comes from Postal's own bundle, which this process loads.
gem 'puma', '~> 6.4'
gem 'sinatra', '~> 4.0'
